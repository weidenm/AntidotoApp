<?xml version="1.0" encoding="utf-8"?>
<!--
   Backup rules for Android 11 and lower (android:fullBackupContent).
   Antídoto is local-first; the default behavior backs up the app data
   directory. Database files are automatically excluded from backup.
   Add <exclude> entries below to opt specific data out of backup.
-->
<full-backup-content>
    <!-- <exclude domain="sharedpref" path="device_id.xml" /> -->
</full-backup-content>
