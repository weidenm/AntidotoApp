<?xml version="1.0" encoding="utf-8"?>
<!--
   Data extraction rules for Android 12+ (android:dataExtractionRules).
   Antídoto is local-first; the default behavior includes the app data
   directory for both cloud backup and device-to-device transfer.
   Add <exclude> entries to opt specific data out.
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- <exclude domain="sharedpref" path="device_id.xml" /> -->
    </cloud-backup>
    <device-transfer>
        <!-- <exclude domain="sharedpref" path="device_id.xml" /> -->
    </device-transfer>
</data-extraction-rules>
