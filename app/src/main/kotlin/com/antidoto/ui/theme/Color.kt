package com.antidoto.ui.theme

import androidx.compose.ui.graphics.Color

// Primary brand colors — green tones (wellness/health)
val PrimaryLight = Color(0xFF2E7D3E)
val PrimaryDark = Color(0xFF66BB6A)

// Secondary colors — blue accents (trust/clarity)
val SecondaryLight = Color(0xFF1565C0)
val SecondaryDark = Color(0xFF64B5F6)

// Tertiary colors — orange (energy/attention)
val TertiaryLight = Color(0xFFE65100)
val TertiaryDark = Color(0xFFFFB74D)

// Background colors
val BackgroundLight = Color(0xFFFAFAFA)
val BackgroundDark = Color(0xFF121212)

// Surface colors
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1E1E1E)

// Error/Warning colors
val ErrorRed = Color(0xFFD32F2F)
val WarningOrange = Color(0xFFF57C00)

// Semantic colors for attention metrics
val AttentionCostRed = Color(0xFFEB5757)
val SuccessGreen = Color(0xFF27AE60)
val NeutralGray = Color(0xFF95A5A6)
